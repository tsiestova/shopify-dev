## Development Guide

### Dev setup

- Run `shopify theme dev` to start local development
- Use `shopify theme pull` / `shopify theme push` to sync changes
- Store connection is configured in `shopify.theme.toml`



